# corner-frontend

Placeholder. The Expo app is scaffolded in **Phase 3** of `docs/BRIEF.md`.

This directory exists now only so the root npm workspace list is complete and
`npm install` resolves. Nothing here is built yet — no navigation graph, no
screens, no services. See `CONVENTIONS.md` for the frontend conventions that
Phase 3 will follow.
